import 'package:flutter/material.dart';

class NatureSounds extends StatelessWidget {
  const NatureSounds({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}